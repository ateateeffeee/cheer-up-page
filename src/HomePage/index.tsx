import React from 'react';
import AdverbGenerator from '../utils/AdverbGenerator';

const Homepage = () => {
    return (
        <div id="home" className="home-page">
            <h1>CHEER THE {AdverbGenerator().toUpperCase()} UP!</h1>
        </div>

        // auto scroll is busted
        // make scrolling thing with spritesheet
        //social links in top thing
        //shop link in nav bar
        //bios for each member
        //instagram video section. auto stop song IF song is playing during that
        //MUSIC tab in nav bar that scrolls to footera
        //color swap. random? toggel? blue and pink? blink?
        //left off at spritesheet thing
    )
}
export default Homepage;